name: interactive_tree_view
description: "A flutter library for creating interactive treeviews and managing tree-like structures."
version: 0.10.1
repository: https://github.com/Yoeri-z/interactive_tree_view
environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.15


flutter: